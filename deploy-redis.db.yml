---

- include: playbooks/ganomede.redis.yml index=0
  when: (config.redis[db_name].deploy | default(true))

- include: playbooks/ganomede.redis.yml index=1
  when: (config.redis[db_name].deploy | default(true))

